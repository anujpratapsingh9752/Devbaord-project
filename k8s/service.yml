apiVersion: v1
kind: Service
metadata:
  name: devboard-fronted-service
  namespace: devboard
spec:
  selector:
    app: devboard-frontend
  ports:
    - protocol: TCP
      port: 30080
      targetPort: 4173
  type: NodePort

