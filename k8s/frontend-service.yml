apiVersion: v1
kind: Service
metadata:
  name: devboard-fronted-service
  namespace: devboard
spec:
  selector:
    app: devboard-frontend
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 4173
      nodePort: 30080
  type: NodePort

